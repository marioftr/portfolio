Drop your source images in this folder (one or multiple). Use distinct filenames for each image (e.g., `galicia_flag.jpg`, `catalonia_flag.jpg`, `skill_aftereffects.png`).

After copying files here, run:

```bash
npm install
npm run images:optimize
```

Optimized images will be written to `src/assets/optimized` with the same filenames.
